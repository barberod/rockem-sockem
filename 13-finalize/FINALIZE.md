---
name: Finalize Commits
phase: execution
step: 13
description: Agent creates granular git commits from unstaged changes. verify-commits.mjs confirms commits were created and contain no attribution.
---

# 📦 Step 13 — Finalize

You are a coding assistant helping a software engineer build and maintain an enterprise-grade product. Prioritize accuracy, professionalism, industry standards, Applied Computer Science principles, and Software Engineering best practices.

{product-text}

## Before

Set the progress state array position 13 (index 12) to `"ACTIVE"`. Render the progress bar:

```bash
node {skill-dir}/progress.mjs '<state-json>'
```

Capture the current HEAD SHA for later verification:

```bash
git -C "{project-repo-location}" rev-parse HEAD
```

Store this as `{head-before}`.

Display to the user:

```
Step 13: Finalize
{bar}
```

## Execute (inference)

Now, you will carefully consider all staged changes in the repo at `{project-repo-location}`, and you will create a fine-grained set of git commits.

You will _not necessarily_ create one commit per changed file; instead, you will form _logical units_ of changes -- just be sure they are _granular_ logical units.

Analyze every staged change with an eye toward specificity. Produce more commits than you normally would — each commit should target a relatively specific change. Name exact methods, classes, properties, or files in the message when practical. Many of these commits should be independently revertable, though this is not a hard requirement for every single one. Every staged change must appear in exactly one commit. No changes may be silently dropped.

### Commit Message Style

Use a simple "verb the noun" format, with a lowercase letter leading. Be specific — name the method, class, property, or file when it adds clarity. Often the verbs will be "add", "edit", "refactor", "enhance", "remove", or "optimize". 90 characters maximum. No message body — subject line only.

#### Examples

```
add StudentId property to EnrollmentResponse contract
rename GetStudentQuery parameter from id to studentId
remove unused System.Linq import from CourseController
add null check for student parameter in ValidateEnrollment
update campus dropdown to use new CampusLookupQuery
edit term date validation in AcademicTermCommandHandler
```

#### No attribution

- **Do not** add agent attribution — no "Co-Authored-By", no "Generated by", no agent names in commit messages

## Verify

After creating commits, run the verification script:

```bash
node {skill-dir}/13-finalize/verify-commits.mjs "{project-repo-location}" "{head-before}"
```

Parse the JSON output:

| Field | Type | Description |
|-------|------|-------------|
| `status` | `"OK"` \| `"SKIP"` \| `"ERROR"` | Verification result |
| `message` | string | Human-readable summary |
| `newCommits` | number | Number of new commits found |
| `commits` | array | List of `{ sha, subject }` for each new commit |
| `errors` | array | Attribution violations or other errors |
| `warnings` | array | Warning messages |

### If `status` is `"ERROR"`

Attribution was found in one or more commits. Amend the offending commit(s) to remove the attribution lines, then re-run verification.

### If `status` is `"SKIP"`

No new commits were found — nothing to finalize (e.g., all comments were rejected). This is expected when Steps 10–11 were skipped.

### If `status` is `"OK"`

Commits are clean. Continue to Step 14.

## After

Set the progress state array position 13 (index 12) to the `status` value from the verification script. Render the progress bar:

```bash
node {skill-dir}/progress.mjs '<state-json>'
```

Display to the user:

```
Step 13: Finalize — {message}
{bar}
```

Where `{message}` is the `message` from the verification result, and `{bar}` is the `bar` from the progress output.
